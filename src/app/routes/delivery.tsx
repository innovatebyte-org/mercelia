import { ContentLayout } from "@/components/layout/content-layout";

export const DeliverySettingsRoute = () => {
  return (
    <ContentLayout title="Delivery Settings">Delivery Settings</ContentLayout>
  );
};
